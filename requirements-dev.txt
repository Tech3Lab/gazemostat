# Development requirements for the Gaze Tracking Application
# Includes all runtime requirements plus development tools
# Install with: pip install -r requirements-dev.txt

# Build tools (required for Python 3.12+ compatibility - install first)
setuptools>=65.0.0     # Provides distutils compatibility (removed in Python 3.12+)
                      # Required for building/installing packages that use distutils

# Include all runtime requirements
-r requirements.txt

# Machine Learning Development (required for generate_model.py)
scikit-learn>=1.0.0    # MultiOutputRegressor wrapper for XGBoost multi-output models
                       # Required for generating models with vector output format
                       # Note: xgboost and joblib are already in requirements.txt

