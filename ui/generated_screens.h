#pragma once

// Canonical OLED UI header (SSD1327 v5).
// Keep this shim so legacy includes of "ui/generated_screens.h" resolve to
// the latest generated UI without changing all include sites.
#include "v5/generated_screens.h"
